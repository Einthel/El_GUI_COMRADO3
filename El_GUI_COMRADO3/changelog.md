# Журнал изменений (Patch Notes)

## 2025-01-17

### ✨ Новые функции (New Features)
- **Динамическая "Фабрика" Виджетов для дисков:**
    - Реализована концепция динамического создания виджетов для отображения информации о дисковых накопителях.
    - Создан новый модуль `storage_widget.py` с классом `StorageWidget`, который является "умной" оберткой над UI-шаблоном `storage_GB.ui`.
    - Каждый виджет диска теперь является независимым компонентом, способным самостоятельно заполнять себя данными через метод `setData()`.
    - Система автоматически создает столько виджетов дисков, сколько найдено в системе, вместо использования заранее заготовленных статических групп.

### ⚙️ Архитектура и рефакторинг (Architecture & Refactoring)
- **Переход от статической к динамической модели:**
    - Полностью переписана логика отображения дисков в `comrado3.py` - удален старый код со статическими группами `storage_groupB_1-4`.
    - Реализован механизм динамической очистки и пересоздания виджетов дисков при каждом обновлении данных.
    - Добавлена программная проверка и создание `QVBoxLayout` для контейнера дисков, что делает код более устойчивым к проблемам с компиляцией `.ui` файлов.
- **Улучшение мониторинга процессора:**
    - Исправлена структура данных в `control_hwinfo.py` - теперь словарь `cpu_data` всегда содержит все ожидаемые ключи (`temp`, `load`, `power`, `clocks`) с значениями по умолчанию `None`.
    - Улучшена обработка `None` значений в `comrado3.py` для предотвращения ошибок форматирования и отображения `nan`.
    - Добавлено однократное логирование всех доступных датчиков процессора для отладки и диагностики совместимости с различным оборудованием.

### 🐛 Исправления (Bug Fixes)
- **Устранение проблем с загрузкой .NET библиотек:**
    - Решена проблема с загрузкой библиотек `LibreHardwareMonitorLib` и `HidSharp` путем удаления конфликтного блока `clr.AddReference`.
    - Система теперь полагается на неявную загрузку через `sys.path`, что устраняет ошибки в логах и обеспечивает стабильную работу мониторинга.
- **Исправление отображения данных процессора:**
    - Устранена проблема с отображением `nan МГц` для частоты процессора.
    - Исправлено отображение нулевых значений температуры и мощности при отсутствии данных от датчиков.
- **Очистка устаревшего кода:**
    - Удален неиспользуемый код установки статических меток `freeTx_storage_label_1-4`, который стал избыточным после перехода на динамическую модель.

### 🔧 Улучшения (Improvements)
- **Оптимизация логирования:**
    - Добавлен флаг `_cpu_sensors_logged` для предотвращения засорения консоли повторными выводами информации о датчиках.
    - Закомментированы избыточные отладочные сообщения WMI для дисков, оставлены только критически важные.
- **Повышение надежности:**
    - Реализована защита от утечек памяти при динамическом создании/удалении виджетов дисков через использование `deleteLater()`.
    - Добавлена проверка существования layout перед его использованием.

Затронутые файлы: `storage_widget.py`, `comrado3.py`, `control_hwinfo.py`, `changelog.md`.
Новые файлы: `storage_widget.py`.

## 2025-10-18

### ✨ Новые функции (New Features)
- **Интеграция мониторинга системы:**
    - Добавлена возможность отображения в реальном времени данных о состоянии процессора на главной странице.
    - Используется библиотека `PyHardwareMonitor` для считывания показателей: модель CPU, температура, общая загрузка, тактовая частота и энергопотребление.
    - Улучшена логика поиска датчиков для повышения совместимости с различным оборудованием (включая процессоры AMD Ryzen и Intel), что обеспечивает более точное отображение температуры и тактовой частоты.
    - **Расширен мониторинг на видеокарту:** Добавлено отображение ключевых показателей видеокарты: температура ядра и Hot Spot, загрузка, частота, энергопотребление, скорость вентиляторов и использование видеопамяти.
- **Гибкий выбор целевого устройства:**
    - На странице настроек добавлен выпадающий список, который автоматически заполняется всеми найденными в системе видеокартами, решая проблему на системах с несколькими GPU.
    - Пользователь может выбрать, какую именно видеокарту отслеживать. Выбор сохраняется в `config.json` и автоматически загружается при старте.
- **Механизм подтверждения сохранения:**
    - Реализована проверка на несохраненные изменения на странице настроек. При попытке уйти со страницы с измененными, но не сохраненными настройками, появляется диалоговое окно с просьбой подтвердить или отменить сохранение.

### ⚙️ Архитектура и рефакторинг (Architecture & Refactoring)
- **Многопоточный сбор данных:**
    - Создан новый модуль `control_hwinfo.py`, отвечающий за сбор данных с датчиков ПК.
    - Вся логика опроса датчиков вынесена в отдельный фоновый поток (`QThread`), что полностью исключает зависание пользовательского интерфейса.
    - Для безопасной передачи данных из фонового потока в основной используется механизм сигналов и слотов Qt.
    - Реализовано корректное завершение фонового потока при закрытии приложения для предотвращения "зависших" процессов.
- **Объединение модулей сохранения:**
    - Функции для работы с пресетами кнопок из `LoadSavePreset.py` были перенесены в основной модуль `LoadSave.py`.
    - Модуль `LoadSavePreset.py` был удален.
    - Это изменение централизует всю логику по загрузке и сохранению конфигурационных файлов проекта в одном месте, упрощая дальнейшую поддержку.

Затронутые файлы: `LoadSave.py`, `editor.py`, `changelog.md`, `control_hwinfo.py`, `comrado3.py`, `main.py`, `save_message_dialog.py`, `constants.py`.
Удаленные файлы: `LoadSavePreset.py`.

## 2025-10-12

### ⚙️ Архитектура, рефакторинг и производительность (Architecture, Refactoring & Performance)
- **Централизация создания теней:**
    - В `utils.py` создана универсальная функция `apply_shadow` для стандартизации и применения эффектов тени (`QGraphicsDropShadowEffect`) к виджетам.
    - Проведен рефакторинг `comrado3.py` и `editor.py`: удалены многочисленные дублирующиеся блоки кода для создания теней, вместо них теперь используются вызовы новой утилиты. Это упрощает поддержку и изменение визуального стиля приложения.
- **Оптимизация управления громкостью:**
    - Унифицировано управление общей системной громкостью. Методы `sound_up` и `sound_down` в `action_button.py` теперь используют готовые функции из `audio_manager.audioController`, устраняя дублирование логики.
    - Проведена глубокая оптимизация производительности в `audio_manager.audioController.py`:
        - Класс `AudioController` теперь кэширует аудиосессию нужного приложения при инициализации, что устраняет ресурсоемкое сканирование всех системных сессий при каждом вызове.
        - Упорядочена работа с COM-интерфейсами: инициализация (`CoInitialize`) теперь выполняется один раз при загрузке модуля, а потенциально опасные вызовы `CoUninitialize` были удалены для повышения стабильности всего приложения.

Затронутые файлы: `utils.py`, `comrado3.py`, `editor.py`, `action_button.py`, `audio_manager/audioController.py`, `changelog.md`.

### 🎨 Улучшения интерфейса (UI Improvements)
- **Улучшена эстетика плеера:**
    - Для кнопок "Следующий" и "Предыдущий трек" добавлен эффект мягкой тени для придания им глубины.
    - Проведена серия визуальных улучшений для обложки альбома, включая эксперименты с формой.
    - Обновлены стили (`Style.qss`) для множества элементов плеера (рамок, меток, фонов) для создания более целостного и отполированного вида.
- **Глубокая стилизация музыкального плеера:**
    - Внедрена новая тёмная цветовая схема, вдохновленная предоставленным дизайном.
    - Проведена полная реорганизация и оптимизация файла `Style.qss` для улучшения читаемости и поддержки.
    - Для всех кнопок управления плеером (`Play`, `Next`, `Previous`, `Random`, `Repeat` и др.) добавлен эффект розового свечения (`QGraphicsDropShadowEffect`) для придания глубины.
    - Добавлен визуальный отклик при нажатии (`:pressed` state) для кнопок навигации, включающий изменение цвета фона, рамки и внутреннего отступа для создания эффекта "утапливания".
    - Проведена тонкая настройка стилей для различных кнопок управления, чтобы визуально разделить основные и вспомогательные элементы.

### 🐛 Исправления (Bug Fixes)
- **Исправлена компоновка в редакторе кнопок:**
    - Устранена критическая проблема со смещением элементов интерфейса в редакторе (`editor.py`), в частности, выпадающего списка шрифтов.
    - Проблема решена путем рефакторинга кода для корректной интеграции кастомных виджетов в существующую компоновку, управляемую Qt Designer, вместо ручного позиционирования. Это обеспечивает стабильное и предсказуемое отображение.

## 2025-10-11

### ✨ Новые функции (New Features)
- **Управление аудиоустройствами Windows:**
    - Реализована полноценная система для управления и переключения аудиоустройств вывода по умолчанию непосредственно из приложения.
    - В настройках добавлена специальная тестовая кнопка для быстрого переключения между основным и вторичным устройством.
- **Интерактивная панель настроек аудио:**
    - На странице настроек реализована панель с выпадающими списками (ComboBox) для выбора основного, вторичного, третьего и четвертого аудиоустройств.
    - Списки автоматически заполняются всеми доступными в системе устройствами.
    - Реализована логика "умного обмена": если выбрать устройство, которое уже используется в другом слоте, они автоматически поменяются местами, предотвращая дублирование.
    - Изменения применяются не мгновенно, а только после нажатия на кнопку "Accept", что позволяет пользователю подтвердить свой выбор.
- **Расширенное управление аудиоустройствами:**
    - В редактор кнопок добавлены новые готовые действия для прямого управления аудиоустройствами (`Переключить Аудио: 1 <-> 2`, `Выбрать Аудио: Устройство 1-4`).
    - Это позволяет назначать переключение или выбор конкретного устройства на любую кнопку.

### 🐛 Исправления (Bug Fixes)
- **Синхронизация плеера с системным аудиовыводом:**
    - Устранена проблема, из-за которой музыкальный плеер продолжал воспроизведение через старое аудиоустройство после его смены в системе.
    - Теперь плеер корректно "подхватывает" новое устройство по умолчанию на лету, сохраняя позицию воспроизведения трека.

### ⚙️ Архитектура и рефакторинг (Architecture & Refactoring)
- **Интеграция модуля `audio_manager`:**
    - В проект интегрирован сторонний модуль для низкоуровневого взаимодействия с COM-интерфейсами звуковой подсистемы Windows (`IPolicyConfig`), что позволило реализовать функционал переключения.
- **Система сигналов для аудиособытий:**
    - В главный обработчик `ActionHandler` добавлен сигнал `audio_device_changed` для централизованного оповещения компонентов о смене аудиоустройства.
    - Модуль `action_button.py` теперь инициирует этот сигнал после успешного переключения, что делает архитектуру более гибкой.
    - Плеер `Music_player.py` теперь "слушает" этот сигнал и автоматически переинициализирует свой аудиовыход (`QAudioOutput`), обеспечивая бесшовный переход звука на новое устройство.
- **Создание управляющего модуля `control_audio.py`:**
    - Написан модуль-обертка, который предоставляет простой и удобный API (`get_all_devices`, `set_device`, `toggle_devices`) для работы со сложной логикой `audio_manager`.
- **"Умная" конфигурация `config.json`:**
    - Реализован механизм, который при запуске приложения автоматически сканирует аудиоустройства в системе.
    - Если секция `audio_settings` в `config.json` отсутствует или не заполнена, программа автоматически заполняет ее найденными устройствами.
    - Система не удаляет из конфигурации устройства, которые были настроены, но временно отключены (например, USB-наушники), и автоматически добавляет новые устройства в свободные слоты.

Затронутые файлы: `comrado3.py`, `control_audio.py`, `config.json`, `requirements.txt`, `changelog.md`, `README.md`, `action_button.py`, `Music_player.py`, `constants.py`.
Добавленные директории: `audio_manager`.

## 2025-09-21

### 🐛 Исправления (Bug Fixes)
- **Исправлена смена иконки Play/Pause:**
    - Устранена давняя проблема, из-за которой иконка кнопки `PlayPause_ToolB` не обновлялась при смене состояния воспроизведения.
    - Проблема решена путем перехода от псевдо-состояния `:checked` к более надежному механизму с использованием динамического свойства `[playMode="on"]` в `Style.qss`, что унифицировало его поведение с другими кнопками плеера.

### 🎨 Улучшения интерфейса (UI Improvements)
- **Анимация прозрачности для кнопки Play/Pause:**
    - Добавлен интерактивный эффект для кнопки `PlayPause_ToolB`. Теперь она по умолчанию полупрозрачна, при нажатии становится полностью видимой, и через 2 секунды плавно возвращается в исходное состояние. Это улучшает визуальную обратную связь.
- **Полная переработка UI плеера:**
    - Интерфейс музыкального плеера был полностью переработан для улучшения эргономики и визуальной привлекательности. Элементы управления сгруппированы более логично, а общая компоновка стала чище и интуитивно понятнее.
- **Позиционирование кнопки Play/Pause в плеере:**
    - Исправлена проблема, из-за которой кнопка Play/Pause смещалась в сторону, а не отображалась поверх обложки трека.
    - Кнопка теперь корректно центрируется и отрисовывается над обложкой, создавая более интуитивный и эстетичный вид плеера.

### ⚙️ Архитектура и рефакторинг (Architecture & Refactoring)
- **Централизация ресурсов иконок:**
    - Для устранения ошибок с относительными путями все системные иконки плеера были перенесены в систему ресурсов Qt (`icons.qrc`).
    - Это гарантирует стабильную загрузку иконок и упрощает управление ресурсами.
- **Очистка проекта:**
    - Удалены старые, неиспользуемые файлы ресурсов (`temp_res.qrc`, `Style.qrc`), что уменьшило количество "мусорных" файлов в проекте.
- **Программное управление компоновкой:**
    - Реализован механизм в `Music_player.py`, который программно управляет положением обложки и кнопки Play/Pause внутри `Cover_Frame`.
    - Этот подход отвязывает позиционирование от менеджеров компоновки Qt Designer, решая проблему перекрытия и обеспечивая точное центрирование.
    - Код был оптимизирован для работы с фреймом фиксированного размера, что устранило избыточную логику отслеживания изменений размера и упростило код.

Затронутые файлы: `Music_player.py`, `Style.qss`, `main.py`, `icons.qrc`, `changelog.md`.
Удаленные файлы: `icons/temp_res.qrc`, `resource/Style.qrc`.

---

## 🚀 Будущий функционал (в разработке)

### ✨ Интеграция с устройствами и сервисами
- **Управление RGB подсветкой (OpenRGB):**
    - Будет добавлена возможность управлять подсветкой комплектующих ПК через приложение OpenRGB. Пользователи смогут назначать на кнопки действия для выключения подсветки или активации сохраненных профилей.
- **Управление Ambient-подсветкой (HyperHDR):**
    - Планируется интеграция с системой HyperHDR для управления фоновой подсветкой. Будет реализована гибкая настройка, позволяющая кнопкам задавать цвет, включать эффекты или выключать подсветку через JSON API.
- **Управление дисплеем корпуса (DeepCool Digital):**
    - Ведется работа над уникальной функцией прямого управления встроенным в корпус дисплеем для мониторинга системных показателей (температура CPU/GPU). Управление будет осуществляться через отдельное окно в системных настройках, работая как фоновый сервис.

---

## 2025-09-19

### 🎨 Улучшения интерфейса (UI Improvements)
- **Центрирование сетки кнопок:**
    - Исправлена проблема неравномерных отступов между кнопками на страницах.
    - Сетка кнопок (`QGridLayout`) теперь размещается внутри дополнительного центрирующего макета, который поглощает лишнее пространство, обеспечивая одинаковые и пропорциональные отступы со всех сторон.
- **Стилизация навигационных вкладок:**
    - В `Style.qss` добавлены уникальные стили (цвет фона, рамка) для четырех основных вкладок навигационной панели: `_1Main_tab`, `_2Button_tab`, `_3Settings_tab`, `_4Music_tab`.
- **Стилизация страницы плеера:**
    - Для страницы музыкального плеера (`Music_page`) задан фоновый цвет для визуального соответствия вкладке навигации.

### ⚙️ Архитектура и рефакторинг (Architecture & Refactoring)
- **Интеграция музыкального плеера:**
    - Логика инициализации и управления музыкальным плеером (`MusicPlayer`) была интегрирована непосредственно в главный обработчик действий (`ActionHandler`) в файле `comrado3.py`.
    - Этот шаг централизует управление плеером и улучшает взаимодействие между основными компонентами приложения.
- **Централизация стилей:**
    - Вся ответственность за визуальное оформление теперь полностью перенесена в `Style.qss` для лучшего разделения логики и представления.

Затронутые файлы: `main.py`, `Style.qss`, `comrado3.py`, `changelog.md`.

## 2025-09-17

### 🚀 Архитектура и рефакторинг (Architecture & Refactoring)
- **Полный редизайн интерфейса:**
    - Приложение переведено на новую концепцию UI, отказавшись от системы динамически размещаемых виджетов в пользу более структурированного макета.
    - Реализована новая компоновка с основной панелью навигации слева и областью для контента справа.
- **Очистка кодовой базы:**
    - Удалены все устаревшие файлы, связанные с предыдущей концепцией виджетов, включая редактор панели (`Editor_bar.py`), загрузчик виджетов (`widget_loader.py`), сами виджеты (`Bar_widget.py`) и их конфигурации.
    - Проведен рефакторинг `main.py` и `comrado3.py` для удаления кода, который управлял старой системой виджетов, что значительно упростило логику и устранило потенциальные ошибки при запуске.

Затронутые файлы: `main.py`, `comrado3.py`, `comrado3.ui`, `changelog.md`.
Удаленные файлы: `Editor_bar.py`, `Editor_bar.ui`, `ui_Editor_bar.py`, `widget_loader.py`, `widget_registry.py`, `config_bar.json`, `Bar_widget.py`, и вся директория `resource/Bar_widget`.

## 2025-09-08

### 🎨 Улучшения интерфейса (UI Improvements)
- **Реализована плавная анимация плейлиста:**
    - Добавлена кнопка для показа/скрытия панели плейлиста.
    - При нажатии, основная панель плеера плавно сдвигается, а панель плейлиста "выезжает" из-под нее, создавая эффект глубины.
    - Анимация сдвига дополнена эффектом плавного появления/исчезновения (fade-in/fade-out) для маскировки момента отрисовки и создания более чистого визуального эффекта.
    - Реализованы настраиваемые отступы для гармоничного расположения элементов в обоих состояниях (открытом и закрытом).
    - Добавлен эффект "резинки" (`OutElastic`) с настроенной амплитудой для более живого и мягкого движения панелей.
    - Улучшена визуальная синхронизация между движением и затуханием плейлиста за счет подбора соответствующих кривых замедления.

### ⚙️ Архитектура и рефакторинг (Architecture & Refactoring)
- **Прямое управление геометрией для анимации:**
    - Для достижения идеальной плавности была изменена стратегия управления виджетами плеера. Вместо использования `QHBoxLayout`, анимация теперь напрямую управляет геометрией (`geometry`) фреймов.
    - Этот подход устранил конфликты между системой компоновки и `QPropertyAnimation`, полностью решив проблему "прыгающих" или резких движений.
- **Синхронизация анимаций:**
    - Движение панелей и изменение их прозрачности синхронизированы с помощью `QParallelAnimationGroup`, что гарантирует одновременное и плавное выполнение всех визуальных эффектов.
- **Надежная инициализация виджетов:**
    - Для корректного позиционирования плеера при запуске был применен отложенный вызов (`QTimer.singleShot`) в `showEvent`. Это решило проблему "гонки состояний", когда виджет пытался спозиционироваться до того, как его родительский контейнер получал финальный размер.
- **Обработка прерываний анимации:**
    - Реализован механизм остановки текущей анимации при новом событии. Это позволяет анимации плавно разворачиваться из своего текущего состояния при быстрых повторных нажатиях на кнопку, устраняя визуальные сбои и "прыжки".

## 2025-09-07

### ✨ Новые функции
- **Интегрирован музыкальный плеер:**
    - В приложение добавлен новый виджет — музыкальный плеер с интеграцией сервиса Яндекс.Музыка.
    - Реализовано воспроизведение треков из плейлиста "Мне нравится".
    - Добавлены базовые элементы управления: Play/Pause, следующий/предыдущий трек.
    - Отображается информация о текущем треке: обложка, название и исполнитель.
    - Реализовано автоматическое переключение на следующий трек после окончания текущего.
- **Функционал "Лайк/Дизлайк":**
    - Кнопка "Лайк" теперь синхронизируется с сервером, добавляя или удаляя треки из плейлиста "Мне нравится".
    - Кнопка "Дизлайк" удаляет трек из "Мне нравится" и немедленно переключает на следующую песню.
    - Статус "лайка" для текущего трека корректно отображается при его загрузке.
- **Режимы воспроизведения:**
    - Добавлены кнопки для управления режимом повтора (выключен, повтор плейлиста, повтор трека).
    - Реализован режим случайного воспроизведения (shuffle).
    - Иконки кнопок динамически меняются в зависимости от выбранного режима.

### 🚀 Архитектура и производительность (Architecture & Performance)
- **Асинхронная загрузка:**
    - Вся логика скачивания треков и обложек была вынесена в фоновые потоки (`QThreadPool`, `QRunnable`).
    - Это полностью устранило зависание интерфейса при переключении треков и сделало приложение отзывчивым.
- **Надежное воспроизведение:**
    - Изменена стратегия воспроизведения: вместо нестабильной потоковой передачи треки теперь полностью скачиваются в буфер в оперативной памяти перед проигрыванием. Это решило проблему с прерыванием песен на середине.

### ⚙️ Рефакторинг (Refactoring)
- **Оптимизация кода:**
    - Устранено дублирование кода в функциях переключения треков (`next_track`, `previous_track`) путем вынесения общей логики в приватный метод `_change_track`.

## 2025-08-13

### 🎨 Улучшения интерфейса (UI Improvements)
- **Устранены артефакты отрисовки:**
    - Полностью решена проблема с "просачиванием" фона в скругленных углах виджетов.
    - Применен подход с "виджетом-оберткой": внешний прозрачный контейнер теперь отвечает за отрисовку тени, а внутренний — за фон и скругление. Это обеспечивает корректное и чистое отображение.
- **Добавлены тени для глубины интерфейса:**
    - Реализован эффект `QGraphicsDropShadowEffect` для придания глубины ключевым элементам интерфейса, включая боковую панель, панели управления страницами и все кнопки на них.
- **Полная стилизация редактора кнопок:**
    - Окно редактора кнопок (`editor.py`) было полностью стилизовано в соответствии с общей темной темой приложения. Настроены все элементы: фон, кнопки, поля ввода, вкладки, выпадающие списки.
- **Централизация шрифтов:**
    - Размеры шрифтов для всех основных элементов (кнопки, метки) вынесены в `Style.qss` для обеспечения единообразия и легкой настройки.

### ⚙️ Рефакторинг и архитектура (Refactoring & Architecture)
- **Динамический заголовок окна:**
    - Реализован механизм, который при запуске автоматически считывает заголовок окна из `comrado3.ui` и устанавливает его в приложении. Теперь заголовок нужно менять только в Qt Designer.
- **Реструктуризация `Style.qss`:**
    - Файл стилей был полностью реорганизован и разделен на 5 логических секций (общие, главное окно, редактор кнопок, редактор панели, виджеты) для улучшения читаемости и упрощения дальнейшей поддержки.

### ✨ Новые функции (New Features)
- **Улучшенный виджет таймера:**
    - Кнопка "Reset" была удалена.
    - Основная кнопка "Timer" теперь работает как переключатель: запускает таймер, меняет свой текст на "Stop" и позволяет остановить отсчет повторным нажатием.

Затронутые файлы: `changelog.md`, `Style.qss`, `comrado3.py`, `main.py`, `utils.py`, `Bar_widget.py`.

## 2025-08-12

### 🚀 Рефакторинг и Архитектура
- **Централизация стилей:**
    - Вся логика, отвечающая за внешний вид (стили), была полностью удалена из Python-кода и `.ui` файлов.
    - Создан единый файл `Style.qss`, который теперь управляет визуальным оформлением всего приложения.
    - Стили из `Style.qss` загружаются динамически при запуске, что обеспечивает чистое разделение между логикой и представлением.
- **Преимущества:**
    - **Упрощение поддержки:** Изменение дизайна теперь требует редактирования только одного файла.
    - **Гибкость:** Упрощено создание новых тем и скинов.
    - **Читаемость кода:** Код на Python стал чище, так как больше не содержит "магических строк" со стилями.

Затронутые файлы: `main.py`, `Style.qss` и практически все файлы `.py`, содержащие UI-логику.

## 2025-08-11

### ✨ Новые функции
- **Полноэкранный режим:**
    - Реализовано переключение основного окна в полноэкранный режим и обратно с помощью специальной кнопки (`Change_Button`).
    - При переходе в полноэкранный режим кнопки редактирования (`Editor_bar_button`, `Editor_button`) становятся неактивными, а кнопка переключения режима подсвечивается как нажатая.
    - При выходе из полноэкранного режима функциональность кнопок восстанавливается.

### 🔧 Улучшения
- **Механизм переключения режимов:**
    - Функция переключения в полноэкранный режим была переработана для использования стандартных методов `showFullScreen()` и `showNormal()` вместо манипуляций с флагами окна. Это обеспечивает более корректное и стабильное поведение на разных системах.

### 🐛 Исправления
- **Поведение при запуске:**
    - Устранена проблема, из-за которой приложение при запуске пыталось автоматически перейти в полноэкранный режим, что приводило к отсутствию рамки окна и кнопок управления.
    - Теперь приложение всегда запускается в стандартном оконном режиме.

## 2025-08-10

### ✨ Новые функции
- **Управление страницами:**
    - Реализован класс `PageManager` для инкапсуляции логики управления страницами (создание, удаление, переключение).
    - В главном окне (`comrado3.py`) добавлены кнопки для навигации (`Next_page`, `Previus_page`) и управления страницами (`Add_page`, `Del_page`).
    - Максимальное количество страниц ограничено константой `MAX_PAGES`.
- **Выполнение системных команд:**
    - В редакторе (`editor.py`) добавлена новая опция для кнопок — "Выполнить команду".
    - Теперь кнопкам можно напрямую назначать выполнение команд в системной оболочке (например, `ipconfig` или `shutdown -a`).

### 🔧 Улучшения
- **Централизация констант:**
    - Создан файл `constants.py` для хранения всех магических чисел и строковых литералов (например, путей к файлам конфигурации и иконок, максимального количества страниц). Это упрощает поддержку и изменение ключевых параметров проекта.
- **Рефакторинг обработчиков действий:**
    - Логика выполнения действий кнопок (запуск программ, горячие клавиши, команды) была вынесена из `comrado3.py` в отдельный, более универсальный модуль `action_button.py`.

## 2025-08-09

### ✨ Новые функции
- **Виджет таймера (`TimerWidget`):**
    - Обратный отсчёт в `Timer_label` с форматированием `mm:ss`.
    - Кнопка `Timer_button` открывает диалог выбора длительности (`Ui_Time_dialog`) с пресетами `1/2/3/5/10/15` минут (кнопки `Timer_buton_1-6`).
    - Кнопка `Timer_reset` останавливает отсчёт и сбрасывает состояние.
- **Интеграция таймера:**
    - Поддержка в редакторе панели (`Editor_bar.py`) и в главном окне (`main.py`).
    - Загрузка/создание по `widget_type: "TimerWidget"` из `config_bar.json`.
- **Поддержка SVG-иконок:**
    - В редакторе кнопок (`editor.py`) добавлена возможность выбирать `.svg`.
    - Отображение SVG через стандартный `QIcon(path)` (Qt рендерит SVG нативно).

### 🔧 Улучшения
- **UX таймера:**
    - Подсветка лейбла на последних 5 сек, короткое выделение по завершении, звуковой сигнал.
    - Кнопка `Reset` активна только во время отсчёта.
    - Защита от повторного открытия диалога, пока он уже открыт.
- **Надёжность создания виджетов:**
    - В `main.py` и `Editor_bar.py` добавлен резервный поиск класса виджета в модуле `Bar_widget`, если `globals()` не содержит нужный класс.
- **Единообразие:**
    - У созданных в главном окне виджетов принудительно выставляется `objectName` по имени класса для удобства отладки.

### 🐛 Исправления
- Стабильное форматирование времени таймера (`mm:ss`) и плавное обновление (4 раза в секунду).
- Небольшие корректировки стилей лейбла таймера для лучшей читаемости.

Затронутые файлы: `Bar_widget.py`, `Editor_bar.py`, `main.py`, `editor.py`, `comrado3.py`, `config_bar.json`.

## 2025-08-03

### ✨ Новые функции
- **Drag & Drop для виджетов в редакторе (`Editor_bar.py`):**
    - Реализована возможность перетаскивать превью виджетов из списка и "бросать" их на специальные целевые панели (`Bar_editor_target_...`).
    - Созданные виджеты корректно размещаются внутри панелей и становятся дочерними элементами, а не плавающими окнами.
- **Перемещение созданных виджетов:**
    - Реализован механизм свободного перетаскивания для всех виджетов (`ClockWidget`, `VolumeWidget` и др.), которые были добавлены на панели. Теперь их можно перемещать мышью внутри родительской панели.

### ⚙️ Внутренние изменения
- **Рефакторинг виджетов (`Bar_widget.py`):**

    - Создан базовый класс `DraggableWidget`, инкапсулирующий всю логику перетаскивания.
    - Все виджеты (`ClockWidget`, `VolumeWidget`) теперь наследуются от `DraggableWidget`, что устранило дублирование кода и упростило архитектуру.
- **Исправление структуры проекта:**
    - Папки `resource` и `resource/Bar_widget` были преобразованы в полноценные Python-пакеты путем добавления файлов `__init__.py`.
    - Устранены некорректные манипуляции с `sys.path`, импорты заменены на абсолютные, что решило проблемы с распознаванием модулей в редакторе.

### 📝 TODO / Что доделать
- **Завершить логику Drag & Drop:**
    - Реализовать сохранение позиций виджетов на панелях в `config.json`.
    - Реализовать загрузку и восстановление виджетов на их местах при открытии редактора.
    - Добавить возможность удаления виджета с панели (например, по правому клику или перетаскиванием за пределы).

## 2025-08-02

### ✨ Новые функции
- **Редактор виджетов для Touch Bar (`Editor_bar.py`):**
    - Создано новое окно "Bar Editor", которое открывается по кнопке из главного меню.
    - Реализован механизм автоматического обнаружения и отображения доступных виджетов из папки `resource/Bar_widget/`.
- **Визуальное представление виджетов:**
    - Вместо простого списка имен, в редакторе теперь отображаются реальные миниатюры (превью) виджетов.
    - Этот механизм динамически создает временный экземпляр каждого виджета, "фотографирует" его и использует полученное изображение как иконку в списке.
    - Список виджетов отображается горизонтально для более удобного просмотра.
- **Интерактивные плавающие виджеты:**
    - Для виджета `ClockWidget` добавлена возможность свободного перетаскивания (Drag and Drop) по экрану с помощью мыши.

### ⚙️ Внутренние изменения
- **Умная компиляция UI:**
    - Система компиляции в `main.py` была полностью переработана. Вместо фиксированного списка файлов она теперь рекурсивно обходит весь проект и автоматически компилирует все найденные `.ui` файлы. Это упрощает добавление новых виджетов и окон.
- **Безопасные импорты:**
    - Устранена циклическая зависимость и ошибки `ImportError`/`ModuleNotFoundError` за счет переноса импортов внутрь методов, где они реально используются (ленивая загрузка). Это гарантирует, что UI-файлы будут скомпилированы до того, как код попытается их импортировать.

## 2025-08-01
- Исправлена проблема с задваиванием/затраиванием обработчиков сигналов кнопок в comrado3.py (теперь перед подключением нового обработчика всегда вызывается disconnect() с обработкой исключения).
- Повышена стабильность работы панели кнопок.
- Теперь можно создавать до 10 страниц с дополнительными кнопками, а также удалять их.
- Полностью переделана структура файла с конфигом: реализовано сохранение страниц и загрузка новых кнопок с них.

## 2024-07-29

### ✨ Новые функции
- **Механика Drag and Drop:**
    - Реализована полноценная поддержка перетаскивания кнопок (Drag and Drop) как в главном окне, так и в редакторе.
    - Конфигурация кнопок (иконки, действия, подписи) автоматически меняется местами при перетаскивании, и изменения сразу сохраняются в `config.json`.
- **Подписи для кнопок:**
    - Добавлена возможность задавать текстовые подписи для кнопок, изменять их шрифт.
    - Подписи отображаются как в главном окне, так и в редакторе, и могут быть настроены индивидуально для каждой кнопки, иконки кнопок находятся по центру, если на кнопке нет подписей
- **Управление пресетами:**
    - Реализована возможность сохранения текущей конфигурации кнопок в качестве пресета. Пользователь может задать имя для пресета через специальное диалоговое окно.
    - При перезаписи кнопки с существующим именем - появляется запрос на перезапись.
    - Добавлена функция загрузки пресетов, что позволяет быстро переключаться между различными раскладками кнопок.
    - Все пресеты сохраняются в виде отдельных `.json` файлов в новой директории `Buttons/`.
    - Пресеты можно удалять или переименовывать.
- **Динамическое обновление интерфейса:**
    - При загрузке пресета иконки и настройки кнопок в главном окне и в редакторе обновляются мгновенно, без необходимости перезапускать приложение.

### 🔧 Улучшения
- **Редактор кнопок (`editor.py`):**
    - **Готовые действия:** Добавлен выпадающий список с готовыми действиями (например, "Открыть Блокнот", "Открыть Калькулятор"), что упрощает настройку кнопок.
    - **Очистка кнопки:** Добавлена кнопка "Очистить", которая сбрасывает все настройки для выбранной кнопки (иконку, действие, подпись).
    - В интерфейс редактора добавлены кнопки "Сохранить пресет" и "Загрузить пресет" для управления конфигурациями.
- **Организация кода:**
    - Создан новый модуль `LoadSavePreset.py` для инкапсуляции всей логики, связанной с загрузкой и сохранением как основной конфигурации (`config.json`), так и пресетов.
    - 


### 🐛 Исправления
- Улучшена стабильность при работе с файлами конфигурации за счет более надежной обработки путей и данных.

## 2024-07-28

### ✨ Новые функции
- **Редактор кнопок (`editor.py`):**
    - Создано окно редактора с нуля, включая базовую структуру и запуск.
    - Реализована загрузка и сохранение конфигурации кнопок (`config.json`).
    - Добавлен динамический поиск кнопок в интерфейсе, что делает код гибким к изменениям в UI.
    - Реализован выбор действия для кнопки:
        - **Горячая клавиша:** Настройка и сохранение сочетаний клавиш.
        - **Запуск программы:** Настройка запуска `.exe` файлов и ярлыков (`.lnk`).
    - Реализована возможность выбора пользовательской иконки для кнопки с предпросмотром.

### 🔧 Улучшения
- **Логика редактора:**
    - Кнопки в редакторе теперь работают в режиме эксклюзивного выбора с возможностью снятия выделения.
    - Панель редактирования становится активной только при выборе кнопки, что улучшает UX.
    - Иконки кнопок в редакторе теперь загружаются из `config.json` при запуске и обновляются мгновенно после сохранения.
- **Основное приложение:**
    - Реализован механизм запуска программ и ярлыков (`.lnk`), назначенных в редакторе.
    
### 🐛 Исправления
- Устранена ошибка `NameError: name 'Q_INT64_C' is not defined`, вызванная некорректной генерацией UI-файла.
- Исправлена логика открытия окна редактора из главного окна, чтобы предотвратить его преждевременное удаление сборщиком мусора Python.
- Добавлена кодировка `utf-8` в `editor.py` для корректного отображения кириллических символов в комментариях.

## 2024-07-27

### 🐛 Исправления
- Устранено предупреждение `RuntimeWarning`, возникавшее при инициализации кнопок. Вместо попытки отсоединить несуществующие сигналы, теперь выполняется проверка на наличие активных подключений, что делает инициализацию чище.

### ⚙️ Внутренние изменения
- **Внешняя конфигурация:** 
    - Создан модуль `LoadSave.py` для управления загрузкой и сохранением настроек кнопок.
    - Вся конфигурация (иконки, действия) вынесена в файл `config.json`. Это позволяет изменять поведение кнопок без модификации кода.
    - Реализовано автоматическое создание `config.json` со структурой по умолчанию, если файл отсутствует при запуске.
- Проведено исследование предупреждения `SetProcessDpiAwarenessContext() failed` в Windows. Проанализированы причины его появления и предложены решения для корректной работы масштабирования интерфейса на экранах с высоким DPI.


## 2024-07-25: Начальная разработка

### ✨ Новые функции
- Создано основное окно приложения с базовой сеткой кнопок.
- Реализован запуск внешних приложений:
    - Веб-браузер
    - Калькулятор
    - Блокнот
- Добавлена функция управления системной громкостью (увеличение/уменьшение).
- Создано пустое окно "Настройки" как задел на будущее.

### 🔧 Улучшения
- Кнопкам добавлены иконки для наглядности.
- Текст на кнопках убран, чтобы интерфейс был чище.

### ⚙️ Внутренние изменения
- Настроена базовая структура проекта.
- Добавлена функция для автоматической компиляции файла `comrado3.ui` в `ui_comrado3.py`, что упрощает разработку интерфейса.


## 2024-07-26

### ✨ Новые функции
- Добавлено новое окно "Редактор" на основе файла `Editor.ui`.

### 🔧 Улучшения
- Окно "Редактор" теперь открывается по нажатию кнопки "Editor" в главном меню.
- Окно "Редактор" сделано модальным: при его открытии главное окно блокируется до закрытия редактора для предотвращения конфликтов.

### ⚙️ Внутренние изменения
- Проведена реорганизация кода для автоматической компиляции нескольких `.ui` файлов (`comrado3.ui` и `Editor.ui`).
- **Рефакторинг:** Вся логика обработки действий кнопок вынесена из `main.py` в новый модуль `action_comrado3.py` для улучшения структуры и читаемости кода. 
